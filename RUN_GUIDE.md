# راهنمای اجرای سرورهای پردازش تصویر (OCR) و مدل‌های محلی (LM Studio)

برای پردازش تصاویر نامه‌ها به سرور OCR نیاز است و برای استخراج فیلدها و اطلاعات ساختاریافته می‌توانید از مدل‌های ابری یا مدل‌های کاملاً آفلاین و محلی LM Studio استفاده کنید.

---

## روش سریع (تک‌کلیک روی دسکتاپ - مانند ویژن‌ویس) 🚀

دو برنامه آماده روی دسکتاپ مک شما قرار گرفته‌اند:
1. **`AutoImport AI.app`**: با دو بار کلیک، مدل شتاب‌دهنده هوش مصنوعی MLX-VLM (روی GPU Metal مک)، سرور OCR محلی (پورت 5151) و LM Studio را فعال کرده و اعلان موفقیت نمایش می‌دهد.
2. **`خاموش کردن AutoImport AI.app`**: با دو بار کلیک، تمامی سرویس‌های OCR و مدل‌ها را متوقف کرده و حافظه رم (Unified Memory) سیستم را به طور کامل آزاد می‌کند.

---

## روش دوم: اجرای دستی از طریق ترمینال روی مک

### مراحل اجرا:
۱. ترمینال را باز کنید.
۲. مطمئن شوید در پوشه اصلی پروژه (`AutoImport-AI`) هستید.
۳. دستور زیر را وارد کرده و اینتر بزنید:

```bash
bash start_ocr.sh
```
یا به صورت دستی:
```bash
python3 local_ocr/server.py
```

* سرور OCR روی آدرس `http://127.0.0.1:5151` بالا می‌آید.
* برای متوقف کردن سرور در ترمینال، کلیدهای `Ctrl + C` را فشار دهید.

---

## روش دوم: اجرای سرور از طریق داکر (Docker OCR)

اگر تمایل دارید سرور را در بستر داکر اجرا کنید تا نیازی به اجرای مستقیم پایتون روی سیستمتان نباشد:

### مراحل اجرا:
۱. ترمینال را باز کنید.
۲. وارد پوشه پروژه داکر شوید (که در کنار پوشه اصلی ساخته شده است):
   ```bash
   cd "../AutoImport-OCR-Docker"
   ```
۳. دستور زیر را برای بیلد و اجرای داکر در پس‌زمینه بزنید:
   ```bash
   docker-compose up -d --build
   ```

* سرور داکری روی پورت `5151` سیستم شما گوش می‌دهد.
* برای دیدن لاگ‌ها و وضعیت اجرای داکر:
   ```bash
   docker-compose logs -f
   ```
* برای خاموش کردن و متوقف کردن داکر:
   ```bash
   docker-compose down
   ```

---

## روش سوم: استفاده از مدل‌های محلی Qwen در LM Studio (آفلاین و پرسرعت)

پروژه AutoImport-AI از ۲ مدل محلی قدرتمند پشتیبانی می‌کند:
1. **`Qwen 3.5 35B`** (`qwen3.5-35b-a3b`)
2. **`Qwen 3.8 27B`** (`qwen3.8-27b@iq2_s`)

### مزایای مدل‌های محلی:
- **کاملاً آفلاین و رایگان:** بدون مصرف حجم یا هزینه API ابری.
- **مدیریت هوشمند حافظه:** افزونه قبل از ارسال درخواست، بررسی می‌کند آیا مدل با کانتکست مناسب (13000) لود شده است یا خیر؛ در صورت وجود مدل غیرفعال دیگر، رم را تخلیه کرده و مدل موردنظر را لود می‌کند.
- **سرعت بالا (Reasoning Off):** درخواست‌ها با پارامتر `reasoning: "off"` ارسال شده تا توکن اضافه و زمان فکر کردن تلف نشود.

### مراحل فعال‌سازی:
۱. نرم‌افزار **LM Studio** را روی مک باز کنید.
۲. به تب **Developer / Local Server** (آیکون فلش‌های دوطرفه `<->`) بروید.
۳. پورت سرور را روی **`1234`** قرار داده و دکمه **Start Server** را بزنید (یا در ترمینال دستور `lms server start` را اجرا کنید).
۴. در پاپ‌آپ افزونه **AutoImport-AI**، تب **تنظیمات** را باز کنید.
۵. وضعیت سرور LM Studio به رنگ **سبز** نمایش داده می‌شود.
۶. در بخش **مدل متنی**، گزینه **`Qwen 3.5 35B (LM Studio محلی)`** یا **`Qwen 3.8 27B`** را انتخاب کرده و روی **ذخیره تنظیمات** کلیک کنید.
